apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: upchar-dev
spec:
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
  type: ClusterIP
