# UPCHAR Shared Library

A shared TypeScript library for common auth/session models, utility functions, and API contracts.

## Use cases
- Reusable DTOs and validation classes
- Authentication and authorization helpers
- Shared response and error models

## Build
Run `npm install` then `npm run build`.
